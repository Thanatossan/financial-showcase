import { Box, Typography } from "@mui/material";

export const DashboardScreen = () => {
    return (
        <Box p={2}>
            <Typography variant="h4">Dashboard</Typography>
            <Typography variant="body1">This is the dashboard screen.</Typography>
        </Box>
    );
}
